---
# defaults for nginx

nodejs_version: "latest"
apt_cache_valid_time: 3600

# defaults file for git

# system level gitconfig settings
git_config_protocol_map :
  - { url: 'ssh://git@github.com', insteadOf : 'git://github.com' }
  - { url: 'https://', insteadOf : 'git://' }

